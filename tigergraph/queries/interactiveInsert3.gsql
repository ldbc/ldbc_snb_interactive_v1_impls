CREATE OR REPLACE QUERY interactiveInsert3(UINT personId, UINT commentId, DATETIME creationDate) {
    INSERT INTO LIKES (FROM, TO, creationDate) VALUES(personId Person, commentId Comment, creationDate);
}